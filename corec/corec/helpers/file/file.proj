GROUP file
{
  USE node
  USE date

  HEADER file.h
  HEADER streams.h

  SOURCE bufstream.c
  SOURCE memstream.c
  SOURCE streams.c
  SOURCE tools.c

  SOURCE(TARGET_LINUX || TARGET_OSX || TARGET_ANDROID) file_libc.c
  SOURCE(TARGET_WIN) file_win32.c
  SOURCE(CONFIG_STDIO) stream_stdio.c
  
  LIBS(TARGET_WIN) shell32.lib
}
